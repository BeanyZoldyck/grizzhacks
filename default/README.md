## Default waiting scenes

This folder contains fallback XML scenes used when no backend XML bundle has
arrived yet:

- `Waiting1.xml`
- `Waiting2.xml`

The backend-driven swapper alternates these as a simple two-frame waiting
animation while it waits for streamed scene XML files.
